import { makeStyles } from "@mui/styles";

export const useFooterStyles = makeStyles({
  footerPaper: {
    height: "60px",
    backgroundColor: "#1976d2",
    boxShadow: "3px 1px 12px 2px rgba(0, 0, 0, 0.3)",
  },
});
